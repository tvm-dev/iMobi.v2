export interface Appointment {
  userId: string;
  appointmentNumber: string;
  description: string;
  location: string;
  link: string;
  status: string;
  observations: string;
  hour: string;
  date: string;

  createdAt: string;
  updatedAt: string;
}
